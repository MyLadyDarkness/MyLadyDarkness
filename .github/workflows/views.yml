name: Update Views

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Release the beast
        uses: antonkomarev/github-profile-views-counter-action@v1.0.9
        with:
          token: ${{ secrets.GH_TOKEN }}
